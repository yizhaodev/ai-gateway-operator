apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: batch-gateway-operator-system

resources:
- ../crd
- ../rbac
- ../manager

images:
- name: controller
  newName: localhost/batch-gateway-operator
  newTag: dev
